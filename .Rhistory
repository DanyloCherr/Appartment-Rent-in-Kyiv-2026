# Стовпець із Дата нам ще знадобиться.
model_75_df_date <- model_df_75[, !names(model_df_75) %in% c("Ч")]
df_long_date <- model_75_df_date %>%
pivot_longer(cols = c(К1, К2, К3),
names_to = "Кімнатність",
values_to = "Ціна") %>%
mutate(Кімнатність = factor(Кімнатність,
levels = c("К1", "К2", "К3"),
labels = c("1", "2", "3")))
head(model_75_df_date)
model_75_df <- model_75_df_date[, -1]
head(model_75_df)
df_long_date <- df_long_date[, -1]
head(df_long_date)
pdata <- pdata.frame(df_long, index = c("Кімнатність", "Дата"))
pdata <- pdata.frame(df_long_date, index = c("Кімнатність", "Дата"))
# Стовпець із Дата нам ще знадобиться.
model_75_df_date <- model_df_75[, !names(model_df_75) %in% c("Ч")]
df_long_date <- model_75_df_date %>%
pivot_longer(cols = c(К1, К2, К3),
names_to = "Кімнатність",
values_to = "Ціна") %>%
mutate(Кімнатність = factor(Кімнатність,
levels = c("К1", "К2", "К3"),
labels = c("1", "2", "3")))
model_75_df <- model_75_df_date[, -1]
df_long <- df_long_date[, -1]
pdata <- pdata.frame(df_long_date, index = c("Кімнатність", "Дата"))
model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
summary(model_pool)
summary(model_Sqrt_75_Lg3)
reset_panel(model_pool)
reset_panel <- function(model, power = 2:3) {
# model — це plm-об'єкт
# power — які степені ŷ додавати (за замовчуванням 2 і 3, як у класичному RESET)
data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, "~ . +", extra_terms))
model_reset <- plm(formula_reset, data = data,
index = model$index, model = "pooling")
# "pooling" - побудувати модель так само, як звичайну (оцінки збігатимуться зі звичайним МНК)
# Робастний тест Вальда
wt <- waldtest(model, model_reset, vcov = vcovCL)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
reset_panel(model_pool)
reset_panel <- function(model, power = 2:3) {
# model — це plm-об'єкт
# power — які степені ŷ додавати (за замовчуванням 2 і 3, як у класичному RESET)
data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, "~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
if (inherits(model, "plm")) {
model_lm <- lm(formula(model), data = data)
} else {
model_lm <- model
}
# Робастний тест Вальда з ЯВНИМ вказанням кластера
wt <- waldtest(model_lm, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)  # явно вказуємо кластер
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
reset_panel(model_pool)
reset_panel <- function(model, power = 2:3) {
# model — це plm-об'єкт
# power — які степені ŷ додавати (за замовчуванням 2 і 3, як у класичному RESET)
data <- model$model
names(data)[1] <- "Y"
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste("Y ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
model_lm <- lm(formula(model), data = data)
wt <- waldtest(model_lm, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
reset_panel(model_pool)
reset_panel <- function(model, power = 2:3) {
# model — це plm-об'єкт
# power — які степені ŷ додавати (за замовчуванням 2 і 3, як у класичному RESET)
data <- model$model
names(data)[1] <- "Y"
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste("Y ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
wt <- waldtest(model, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
reset_panel(model_Sqrt_75_Lg3)
reset_panel <- function(model, power = 2:3) {
# model — це plm-об'єкт
# power — які степені ŷ додавати (за замовчуванням 2 і 3, як у класичному RESET)
if (inherits(model, "lm") && !inherits(model, "plm")) {
# Створити pdata.frame
pdata <- pdata.frame(model$model, index = c("Кімнатність", "Дата"))
model <- plm(formula(model), data = pdata, model = "pooling")
}
data <- model$model
names(data)[1] <- "Y"
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste("Y ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
model_lm <- lm(Y ~ ., data = data[, !grepl("y_hat_", names(data))])  # базова без ŷ², ŷ³
wt <- waldtest(model_lm, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3)
reset_panel <- function(model, data, cluster_var = "Кімнатність", power = 2:3) {
# data — оригінальний датафрейм (df_long)
# cluster_var — змінна для кластеризації
model_data <- model$model
names(model_data)[1] <- "Y"
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
model_data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste("Y ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = model_data)
model_lm <- lm(Y ~ ., data = model_data[, !grepl("y_hat_", names(model_data))])
wt <- waldtest(model_lm, model_reset,
vcov = vcovCL,
cluster = data[[cluster_var]])  # ← кластер із оригінальних даних
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
cat(ifelse(wt$`Pr(>F)`[2] > 0.05,
"\n✅ Лінійність не відхиляється",
"\n❌ Виявлено нелінійність"))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long_date)
reset_panel <- function(model, data, cluster_var = "Кімнатність", power = 2:3) {
model_data <- model$model
y_name <- names(model_data)[1]          # "sqrt(Ціна)" — оригінальна назва
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
model_data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, "~ . +", extra_terms))
model_reset <- lm(formula_reset, data = model_data)
# Базова модель — ТА САМА формула, що й у model_reset, але без y_hat
model_base <- update(model_reset, . ~ . - y_hat_2 - y_hat_3)
wt <- waldtest(model_base, model_reset,
vcov = vcovCL,
cluster = data[[cluster_var]])
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
cat(ifelse(wt$`Pr(>F)`[2] > 0.05,
"\n✅ Лінійність не відхиляється",
"\n❌ Виявлено нелінійність"))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long_date)
reset_panel <- function(model, power = 2:3) {
data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
model_lm <- lm(formula(model), data = data)
wt <- waldtest(model_lm, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
cat(ifelse(wt$`Pr(>F)`[2] > 0.05,
"\n✅ Лінійність не відхиляється",
"\n❌ Виявлено нелінійність"))
invisible(list(waldtest = wt, model_reset = model_reset))
}
reset_panel <- function(model, power = 2:3) {
data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
model_lm <- lm(formula(model), data = data)
wt <- waldtest(model_lm, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long_date)
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3)
model_Sqrt_75_Lg3$model
head(model_Sqrt_75_Lg3$model)
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3)
reset_panel <- function(model, data, power = 2:3) {
#data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
model_lm <- lm(formula(model), data = data)
wt <- waldtest(model_lm, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
reset_panel <- function(model, data, power = 2:3) {
#data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
wt <- waldtest(model, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
reset_panel <- function(model, data, power = 2:3) {
#data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
# model_reset <- lm(formula_reset, data = data)
model_reset <- lm(Ціна ~ ., data = data)
wt <- waldtest(model, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
reset_panel <- function(model, data, power = 2:3) {
#data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
print(class(model))
print(class(model_reset))
# wt <- waldtest(model, model_reset,
#                vcov = vcovCL,
#                cluster = data$Кімнатність)
#
# cat("=== RESET-тест для панельних даних ===\n")
# cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
# cat(sprintf("Статистика: %.4f\n", wt$F[2]))
# cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
#
# invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
reset_panel <- function(model, data, power = 2:3) {
#data <- model$model
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
wt <- waldtest(model, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
reset_panel <- function(model, data, power = 2:3) {
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
wt <- waldtest(model, model_reset,
vcov = vcovCL,
cluster = data$Кімнатність)
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
reset_panel <- function(model, data, power = 2:3) {
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, " ~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
wt <- waldtest(model, model_reset,
vcov = vcovCL,
cluster = data[["Кімнатність"]])
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
reset_panel <- function(model, data, power = 2:3) {
# Беремо дані з моделі (вони вже містять sqrt(Ціна))
model_data <- model$model
y_name <- names(model_data)[1]
y_hat <- fitted(model)
# Додаємо ŷ², ŷ³
for (k in power) {
col_name <- paste0("y_hat_", k)
model_data[[col_name]] <- y_hat^k
}
# 1. Базова модель (ТІ САМІ дані, що й model_reset)
# Видаляємо тільки y_hat_* з формули
all_vars <- names(model_data)
base_vars <- setdiff(all_vars, paste0("y_hat_", power))
formula_base <- as.formula(paste(y_name, "~", paste(base_vars[-1], collapse = " + ")))
model_base <- lm(formula_base, data = model_data)
# 2. Розширена модель
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, "~ . +", extra_terms))
model_reset <- lm(formula_reset, data = model_data)
# Кластер із data (df_long)
cluster_vec <- data[["Кімнатність"]]
wt <- waldtest(model_base, model_reset,
vcov = function(x) vcovCL(x, cluster = cluster_vec))
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
cat(ifelse(wt$`Pr(>F)`[2] > 0.05,
"\n✅ Лінійність не відхиляється",
"\n❌ Виявлено нелінійність"))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
reset_panel <- function(model, data, power = 2:3) {
y_name <- names(data)[1]
y_hat <- fitted(model)
for (k in power) {
col_name <- paste0("y_hat_", k)
data[[col_name]] <- y_hat^k
}
all_vars <- names(data)
base_vars <- setdiff(all_vars, paste0("y_hat_", power))
formula_base <- as.formula(paste(y_name, "~", paste(base_vars[-1], collapse = " + ")))
model_base <- lm(formula_base, data = data)
# 2. Розширена модель
extra_terms <- paste(paste0("y_hat_", power), collapse = " + ")
formula_reset <- as.formula(paste(y_name, "~ . +", extra_terms))
model_reset <- lm(formula_reset, data = data)
# Кластер із data (df_long)
cluster_vec <- data[["Кімнатність"]]
wt <- waldtest(model_base, model_reset,
vcov = function(x) vcovCL(x, cluster = cluster_vec))
cat("=== RESET-тест для панельних даних ===\n")
cat("Додано:", paste0("ŷ^", power, collapse = ", "), "\n")
cat(sprintf("Статистика: %.4f\n", wt$F[2]))
cat(sprintf("p-value:    %.4f\n", wt$`Pr(>F)`[2]))
invisible(list(waldtest = wt, model_reset = model_reset))
}
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long)
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long, power = 2)
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long, power = 1)
# model_pool <- plm(formula(model_Sqrt_75_Lg3) , data = pdata, model = "pooling")
#
reset_panel(model_Sqrt_75_Lg3, df_long, power = 2)
